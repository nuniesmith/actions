# =================================================================
# Netdata Cloud Configuration Template
# =================================================================
# Copy this to your .env file and update with your actual values
# 
# To get these values:
# 1. Go to https://app.netdata.cloud
# 2. Sign up or sign in
# 3. Create a new space/room or use existing one
# 4. Go to "Connect Nodes" to get your claim token and room ID

# Netdata Cloud claim token (required for cloud integration)
NETDATA_CLAIM_TOKEN=your_claim_token_here

# Netdata Cloud claim URL (usually doesn't need to be changed)
NETDATA_CLAIM_URL=https://app.netdata.cloud

# Netdata Cloud room ID (where your nodes will appear)
NETDATA_CLAIM_ROOMS=your_room_id_here

# Optional: Disable telemetry (set to 1 to disable)
NETDATA_DISABLE_TELEMETRY=1

# Optional: Extra packages for enhanced monitoring
NETDATA_EXTRA_DEB_PACKAGES=
NETDATA_EXTRA_APK_PACKAGES=

# =================================================================
# GitHub Secrets Configuration
# =================================================================
# Add these secrets to your GitHub repository:
# 
# Repository → Settings → Secrets and variables → Actions → New repository secret
# 
# Secret Name: NETDATA_CLAIM_TOKEN
# Secret Value: [your claim token from Netdata Cloud]
# 
# Secret Name: NETDATA_CLAIM_ROOM  
# Secret Value: [your room ID from Netdata Cloud]
